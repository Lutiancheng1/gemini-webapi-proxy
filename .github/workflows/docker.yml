name: Docker

on:
  push:
    branches: [main]
    tags: ["v*"]
  pull_request:
    paths:
      - "Dockerfile"
      - ".dockerignore"
      - "pyproject.toml"
      - "src/**"
      - "uv.lock"

permissions:
  contents: read
  packages: write

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}

jobs:
  # Build each platform in its own job.  Each job pushes ONLY its
  # own per-platform image (tagged with a `-<platform>` suffix)
  # plus a unique digest, never a user-facing tag.  The `merge`
  # job below then combines the per-platform images into a
  # multi-arch manifest list and re-tags it with the user-facing
  # tags.  This avoids the previous bug where each matrix job
  # pushed the same user-facing tag and the later push overwrote
  # the earlier manifest list with a single-platform image.
  build:
    name: Build (${{ matrix.platform }})
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        platform: [linux/amd64, linux/arm64]
    steps:
      - uses: actions/checkout@v4

      - uses: docker/setup-qemu-action@v3
        with:
          platforms: ${{ matrix.platform }}

      - uses: docker/setup-buildx-action@v3

      - name: Log in to GHCR
        if: github.event_name != 'pull_request'
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
          tags: |
            type=ref,event=branch
            type=ref,event=pr
            type=semver,pattern={{version}}
            type=semver,pattern={{major}}.{{minor}}
            type=raw,value=latest,enable={{is_default_branch}}

      - name: Build & push (per-platform, no user tags)
        uses: docker/build-push-action@v6
        with:
          context: .
          platforms: ${{ matrix.platform }}
          push: ${{ github.event_name != 'pull_request' }}
          # Only the per-platform staging tag — never a user-facing
          # tag.  The merge job below creates the manifest list.
          # Note: tag component cannot contain '/', so we replace
          # the platform slash with a dash via the `slug` step below.
          tags: ${{ steps.slug.outputs.staging }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Compute staging tag (no '/' in tag component)
        id: slug
        run: |
          platform_dash=$(echo "${{ matrix.platform }}" | tr '/' '-')
          staging="${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ steps.meta.outputs.version }}-${platform_dash}"
          echo "staging=${staging}" >> "$GITHUB_OUTPUT"
          echo "staging=${staging}"
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

  merge:
    name: Merge multi-arch manifest
    needs: build
    if: github.event_name != 'pull_request'
    runs-on: ubuntu-latest
    steps:
      - uses: docker/setup-buildx-action@v3

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
          tags: |
            type=ref,event=branch
            type=ref,event=pr
            type=semver,pattern={{version}}
            type=semver,pattern={{major}}.{{minor}}
            type=raw,value=latest,enable={{is_default_branch}}

      - name: Build manifest list and apply user tags
        env:
          DOCKER_CLI_EXPERIMENTAL: enabled
        run: |
          set -euo pipefail
          version="${{ steps.meta.outputs.version }}"
          image_repo="${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}"
          # Per-platform images (staging tags) were pushed by the
          # build matrix.  Tag component cannot contain '/', so
          # we replace the platform slash with a dash.
          manifest_args=()
          for plat in linux/amd64 linux/arm64; do
            plat_dash=$(echo "$plat" | tr '/' '-')
            staging="${image_repo}:${version}-${plat_dash}"
            echo "Including staging image: $staging"
            manifest_args+=("--image" "$staging")
          done

          # Apply every user-facing tag from metadata-action to the
          # combined multi-arch manifest list.
          for tag in ${{ steps.meta.outputs.tags }}; do
            echo "Creating manifest list $tag"
            docker buildx imagetools create \
              "${manifest_args[@]}" \
              --tag "$tag" \
              --provenance=false
          done
          # Drop the per-platform staging tags — they're no longer
          # useful and add noise to the package listing.
          for plat in linux/amd64 linux/arm64; do
            plat_dash=$(echo "$plat" | tr '/' '-')
            docker buildx imagetools rm -f "${image_repo}:${version}-${plat_dash}" || true
          done
